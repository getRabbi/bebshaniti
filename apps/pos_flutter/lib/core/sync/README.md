# Offline sync boundary

Phase 6 implements a local outbox, bootstrap/pull cursors, idempotent push, retry policy, and connectivity scheduling here. Completed sales will synchronize as immutable events.
